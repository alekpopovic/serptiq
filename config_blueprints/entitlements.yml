schema_version: 1
definitions:
- { key: projects.max, value_type: integer, unit: projects, category: resources, minimum: 0, maximum: 1000000, allowed_values: [], max_length: ~, allow_custom: true, security_sensitive: false, system_default: 0, description: "Maximum active projects" }
- { key: members.max, value_type: integer, unit: members, category: resources, minimum: 0, maximum: 1000000, allowed_values: [], max_length: ~, allow_custom: true, security_sensitive: false, system_default: 0, description: "Maximum active organization memberships" }
- { key: teams.max, value_type: integer, unit: teams, category: resources, minimum: 0, maximum: 1000000, allowed_values: [], max_length: ~, allow_custom: true, security_sensitive: false, system_default: 0, description: "Maximum active teams" }
- { key: roles.custom, value_type: boolean, unit: capability, category: authorization, minimum: ~, maximum: ~, allowed_values: [], max_length: ~, allow_custom: false, security_sensitive: true, system_default: false, description: "Custom organization roles" }
- { key: website_properties.max, value_type: integer, unit: properties, category: resources, minimum: 0, maximum: 1000000, allowed_values: [], max_length: ~, allow_custom: true, security_sensitive: false, system_default: 0, description: "Maximum active website properties" }
- { key: mobile_properties.max, value_type: integer, unit: properties, category: resources, minimum: 0, maximum: 1000000, allowed_values: [], max_length: ~, allow_custom: true, security_sensitive: false, system_default: 0, description: "Maximum active mobile properties" }
- { key: crawl.manual, value_type: boolean, unit: capability, category: crawling, minimum: ~, maximum: ~, allowed_values: [], max_length: ~, allow_custom: false, security_sensitive: false, system_default: false, description: "Manual scans" }
- { key: crawl.credits_monthly, value_type: integer, unit: credits, category: crawling, minimum: 0, maximum: 1000000000000, allowed_values: [], max_length: ~, allow_custom: true, security_sensitive: false, system_default: 0, description: "Weighted monthly analysis credits" }
- { key: crawl.max_urls_per_scan, value_type: integer, unit: urls, category: crawling, minimum: 0, maximum: 1000000000, allowed_values: [], max_length: ~, allow_custom: true, security_sensitive: false, system_default: 0, description: "Maximum URLs in one scan" }
- { key: crawl.concurrent_scans, value_type: integer, unit: scans, category: crawling, minimum: 0, maximum: 100000, allowed_values: [], max_length: ~, allow_custom: true, security_sensitive: false, system_default: 0, description: "Concurrent running scans" }
- { key: crawl.schedule, value_type: enum, unit: cadence, category: crawling, minimum: ~, maximum: ~, allowed_values: [none, weekly, daily, hourly], max_length: ~, allow_custom: true, security_sensitive: false, system_default: none, description: "Fastest recurring scan schedule" }
- { key: crawl.javascript_rendering, value_type: boolean, unit: capability, category: crawling, minimum: ~, maximum: ~, allowed_values: [], max_length: ~, allow_custom: false, security_sensitive: true, system_default: false, description: "Isolated browser rendering" }
- { key: crawl.max_rendered_pages_per_scan, value_type: integer, unit: pages, category: crawling, minimum: 0, maximum: 10000000, allowed_values: [], max_length: ~, allow_custom: true, security_sensitive: false, system_default: 0, description: "Maximum rendered pages in one scan" }
- { key: crawl.targeted_rescan, value_type: boolean, unit: capability, category: crawling, minimum: ~, maximum: ~, allowed_values: [], max_length: ~, allow_custom: false, security_sensitive: false, system_default: false, description: "Targeted URL verification rescans" }
- { key: crawl.custom_user_agent, value_type: boolean, unit: capability, category: crawling, minimum: ~, maximum: ~, allowed_values: [], max_length: ~, allow_custom: false, security_sensitive: true, system_default: false, description: "Organization crawler suffix" }
- { key: crawl.custom_rules, value_type: boolean, unit: capability, category: crawling, minimum: ~, maximum: ~, allowed_values: [], max_length: ~, allow_custom: false, security_sensitive: false, system_default: false, description: "Configurable supported rule thresholds" }
- { key: data.normalized_retention_days, value_type: integer, unit: days, category: data, minimum: 0, maximum: 36500, allowed_values: [], max_length: ~, allow_custom: true, security_sensitive: false, system_default: 0, description: "Normalized scan data retention" }
- { key: data.raw_artifact_retention_days, value_type: integer, unit: days, category: data, minimum: 0, maximum: 36500, allowed_values: [], max_length: ~, allow_custom: true, security_sensitive: false, system_default: 0, description: "Raw artifact retention" }
- { key: data.export, value_type: boolean, unit: capability, category: data, minimum: ~, maximum: ~, allowed_values: [], max_length: ~, allow_custom: false, security_sensitive: true, system_default: false, description: "Organization data export" }
- { key: search_console.enabled, value_type: boolean, unit: capability, category: integrations, minimum: ~, maximum: ~, allowed_values: [], max_length: ~, allow_custom: false, security_sensitive: false, system_default: false, description: "Google Search Console connection" }
- { key: search_console.history_months, value_type: integer, unit: months, category: integrations, minimum: 0, maximum: 1200, allowed_values: [], max_length: ~, allow_custom: true, security_sensitive: false, system_default: 0, description: "Retained Search Analytics history" }
- { key: url_inspection.enabled, value_type: boolean, unit: capability, category: integrations, minimum: ~, maximum: ~, allowed_values: [], max_length: ~, allow_custom: false, security_sensitive: false, system_default: false, description: "Google URL Inspection imports" }
- { key: url_inspection.monthly_urls, value_type: integer, unit: urls, category: integrations, minimum: 0, maximum: 1000000000, allowed_values: [], max_length: ~, allow_custom: true, security_sensitive: false, system_default: 0, description: "Monthly URL Inspection allowance" }
- { key: performance.crux, value_type: boolean, unit: capability, category: performance, minimum: ~, maximum: ~, allowed_values: [], max_length: ~, allow_custom: false, security_sensitive: false, system_default: false, description: "CrUX field data" }
- { key: performance.lighthouse, value_type: boolean, unit: capability, category: performance, minimum: ~, maximum: ~, allowed_values: [], max_length: ~, allow_custom: false, security_sensitive: false, system_default: false, description: "Lighthouse lab analysis" }
- { key: performance.lighthouse_pages_per_scan, value_type: integer, unit: pages, category: performance, minimum: 0, maximum: 10000000, allowed_values: [], max_length: ~, allow_custom: true, security_sensitive: false, system_default: 0, description: "Lighthouse page sample cap" }
- { key: ai_crawlers.policy_matrix, value_type: boolean, unit: capability, category: crawling, minimum: ~, maximum: ~, allowed_values: [], max_length: ~, allow_custom: false, security_sensitive: false, system_default: false, description: "AI crawler policy matrix" }
- { key: app_discovery.deep_links, value_type: boolean, unit: capability, category: discovery, minimum: ~, maximum: ~, allowed_values: [], max_length: ~, allow_custom: false, security_sensitive: false, system_default: false, description: "Deep-link validation" }
- { key: app_discovery.store_audit, value_type: boolean, unit: capability, category: discovery, minimum: ~, maximum: ~, allowed_values: [], max_length: ~, allow_custom: false, security_sensitive: false, system_default: false, description: "App-store metadata audit" }
- { key: app_discovery.route_map, value_type: boolean, unit: capability, category: discovery, minimum: ~, maximum: ~, allowed_values: [], max_length: ~, allow_custom: false, security_sensitive: false, system_default: false, description: "Web-to-app route mapping" }
- { key: releases.enabled, value_type: boolean, unit: capability, category: releases, minimum: ~, maximum: ~, allowed_values: [], max_length: ~, allow_custom: false, security_sensitive: false, system_default: false, description: "Release records" }
- { key: releases.regression_gate, value_type: boolean, unit: capability, category: releases, minimum: ~, maximum: ~, allowed_values: [], max_length: ~, allow_custom: false, security_sensitive: false, system_default: false, description: "Automated regression evaluation" }
- { key: releases.blocking_gate, value_type: boolean, unit: capability, category: releases, minimum: ~, maximum: ~, allowed_values: [], max_length: ~, allow_custom: false, security_sensitive: true, system_default: false, description: "Explicit blocking CI status" }
- { key: reports.html, value_type: boolean, unit: capability, category: reporting, minimum: ~, maximum: ~, allowed_values: [], max_length: ~, allow_custom: false, security_sensitive: false, system_default: false, description: "Immutable HTML reports" }
- { key: reports.pdf, value_type: boolean, unit: capability, category: reporting, minimum: ~, maximum: ~, allowed_values: [], max_length: ~, allow_custom: false, security_sensitive: false, system_default: false, description: "PDF report artifact" }
- { key: reports.scheduled, value_type: boolean, unit: capability, category: reporting, minimum: ~, maximum: ~, allowed_values: [], max_length: ~, allow_custom: false, security_sensitive: false, system_default: false, description: "Scheduled report delivery" }
- { key: reports.white_label, value_type: boolean, unit: capability, category: reporting, minimum: ~, maximum: ~, allowed_values: [], max_length: ~, allow_custom: false, security_sensitive: false, system_default: false, description: "White-label report branding" }
- { key: notifications.email, value_type: boolean, unit: capability, category: notifications, minimum: ~, maximum: ~, allowed_values: [], max_length: ~, allow_custom: false, security_sensitive: false, system_default: false, description: "Email notifications" }
- { key: notifications.slack, value_type: boolean, unit: capability, category: notifications, minimum: ~, maximum: ~, allowed_values: [], max_length: ~, allow_custom: false, security_sensitive: false, system_default: false, description: "Slack notifications" }
- { key: integrations.outgoing_webhooks, value_type: boolean, unit: capability, category: integrations, minimum: ~, maximum: ~, allowed_values: [], max_length: ~, allow_custom: false, security_sensitive: true, system_default: false, description: "Signed outgoing webhooks" }
- { key: api.access, value_type: enum, unit: access_level, category: developer, minimum: ~, maximum: ~, allowed_values: [none, read, read_write], max_length: ~, allow_custom: true, security_sensitive: true, system_default: none, description: "Public API access level" }
- { key: audit.retention_days, value_type: integer, unit: days, category: audit, minimum: 0, maximum: 36500, allowed_values: [], max_length: ~, allow_custom: true, security_sensitive: false, system_default: 0, description: "Customer-visible audit retention" }
- { key: audit.export, value_type: boolean, unit: capability, category: audit, minimum: ~, maximum: ~, allowed_values: [], max_length: ~, allow_custom: false, security_sensitive: true, system_default: false, description: "Audit export" }
- { key: security.sso_saml, value_type: boolean, unit: capability, category: security, minimum: ~, maximum: ~, allowed_values: [], max_length: ~, allow_custom: false, security_sensitive: true, system_default: false, description: "SAML single sign-on" }
- { key: security.ip_allowlist, value_type: boolean, unit: capability, category: security, minimum: ~, maximum: ~, allowed_values: [], max_length: ~, allow_custom: false, security_sensitive: true, system_default: false, description: "Organization IP allowlist" }
- { key: operations.dedicated_workers, value_type: boolean, unit: capability, category: operations, minimum: ~, maximum: ~, allowed_values: [], max_length: ~, allow_custom: false, security_sensitive: true, system_default: false, description: "Dedicated worker pool" }
- { key: support.level, value_type: enum, unit: support_level, category: support, minimum: ~, maximum: ~, allowed_values: [community, standard, priority, contract], max_length: ~, allow_custom: false, security_sensitive: false, system_default: community, description: "Support service level" }
