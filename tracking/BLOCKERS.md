# Blockers

The CLI appends blocked prompt events here. Resolve the underlying issue, then use `unblock`; do not delete history.

| Timestamp (UTC) | Prompt | Blocker | Resolution |
|---|---:|---|---|
