#!/usr/bin/env ruby
require "rubygems"
require "bundler/setup"

# The lockfile and dependency update workflow own scanner freshness. Avoid a
# network-dependent latest-version check inside otherwise deterministic scans.

load Gem.bin_path("brakeman", "brakeman")
